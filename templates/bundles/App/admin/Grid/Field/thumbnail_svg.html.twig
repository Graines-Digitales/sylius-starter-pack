{% if data.iconMedia.filename is defined and data.iconMedia.filename is not empty %}
    <div class="preview_icon">
        {% include '@App/web/components/svg/' ~ data.iconMedia.filename ~ '.twig' ignore missing %}
    </div>
{% endif %}