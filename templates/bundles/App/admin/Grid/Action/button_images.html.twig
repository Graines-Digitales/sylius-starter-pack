{% import '@SyliusUi/Macro/buttons.html.twig' as buttons %}


{% set path = options.link.url|default(path(options.link.route|default(grid.requestConfiguration.getRouteName('duplicate')), options.link.parameters|default({'id': data.id}))) %}
{{ buttons.default(path, action.label, null, 'clone', 'orange') }}

