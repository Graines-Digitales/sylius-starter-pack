
<h4 class="ui horizontal divider header">
    <i class="info icon"></i>
    {{ 'app.ui.form.page_general' | trans }}
</h4>
<div class="ui segment">
        {{ form_errors(form) }}
        {# Issue with Symfony 5, Sylius 1.9 : https://github.com/Sylius/Sylius/pull/12439 #}
        {# So we render the fields #}
        {{ form_row(form.isEnabled) }}
        
        {# {{ form_row(form.primaryImage) }} #}
        {{ form_row(form.name) }}
        {{ form_row(form.url) }}
		{{ form_row(form.icon) }}
        {{ form_row(form.category, {'label':false}) }}
        {{ form_row(form._category) }}
        {# {{ form_row(form.type) }} #}
        {# {{ form_row(form.icon) }} #}


		{# <div class="ui two column divided grid"> 
			<div class="row">
				<div class="column">
					{{ form_row(form.primaryImage) }}
				</div>
				<div class="column">
				{% if social_links.primaryImage is not null and social_links.primaryImage.filename is not null and social_links.primaryImage.filename != '' %}
					<div class="ui centered medium image">
						<img src="{{ asset(directory_image_media_object ~ '/' ~ social_links.primaryImage.filename) }}">
					</div>
				{% endif %}
				</div>
			</div>
		</div> #}
	
	{# Instead of #}
	{# {{ form_rest(form) }} #}
</div>
